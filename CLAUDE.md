# CLAUDE.md

気象庁防災情報XML（PULL型）を毎分ポーリングし、**日本国内全域**の防災情報から
通知すべきものを選んで届けるシステム。
Google Cloud（Cloud Functions gen2 + Pub/Sub + Cloud Scheduler + Firestore）で構成する。

## このシステムの最大の設計課題

**取り込みは全国全件、通知は厳しく絞る。** ここを取り違えると失敗する。

気象警報・注意報は府県予報区ごと（全国で約140区）に発表され、荒天時はそれぞれが
短時間に何度も更新される。地震も1回の揺れで震度速報・震源に関する情報・震源震度情報と
複数の電文が連続する。全件を素通しで通知すると、悪天候の日には数百通が並び、
**通知が多すぎて誰も見なくなる**。これが本システムで最も現実的な失敗モードである。

したがって、通知量を減らすための以下3つの仕組みは、あとから足す機能ではなく
最初から設計に組み込む。

### 1. イベント通知ではなく状態差分

気象警報・注意報の電文は差分ではなく、**その府県予報区の現在の全状態**が毎回入っている。
素直に処理すると「変化のない再送」まで通知してしまう。

府県予報区ごとの現在状態（発表中の警報・注意報の集合）を Firestore に保持し、
前回状態と比較して **新規発表 / 格上げ / 格下げ / 解除** が起きたときだけ通知する。

### 2. 深刻度による通知経路の分岐

| 段階 | 対象 | 扱い |
| --- | --- | --- |
| 即時・個別 | 特別警報、津波警報・注意報、噴火警報、震度5弱以上、レベル4の各危険警報 | 1件ずつ即通知 |
| 集約 | 警報クラス（レベル3相当、暴風・波浪・大雪等の警報） | 5〜15分ごとに1通へまとめる |
| 記録のみ | 注意報、定時情報 | Firestore に記録、通知しない |

段階の境界は運用しながら調整する。閾値は設定として外に出し、コードに埋めない。

### 3. EventID による束ね

地震の続報は `Head/EventID` が同一。1事象＝1通知として扱い、続報はメッセージの更新
（Slack なら `chat.update`）で反映する。新規メッセージを積まない。

## 絶対に守る制約

### 気象庁サーバーへのアクセス量

1日10GB以上のダウンロードが検知されるとIPアドレスが遮断される。全国運用でも電文の総量は
1日数百MB程度に収まる見込みだが、以下は交渉の余地のない要件とする。

- フィード取得は必ず条件付きGET（`If-None-Match` / `If-Modified-Since`）を行い、
  304 Not Modified で早期リターンする
- **一度取得した電文URLは二度取得しない。** 重複排除を通過したものだけが本体を取りに行く
- ポーリング間隔は最短1分。それより短くしない
- 電文取得の同時接続数は5程度に制限する（並列化しても気象庁側に殺到させない）
- `--max-instances` を設定し、暴走時の被害を抑える
- 開発・テスト中に実サーバーへ繰り返しアクセスしない（フィクスチャを使う）

### 学習データ上の情報名を信用しない

2026年5月29日に防災気象情報の体系が大きく変わり、大雨・土砂災害・河川氾濫・高潮の情報名が
「レベル3大雨警報」「レベル4土砂災害危険警報」「レベル5土砂災害特別警報」等に変更され、
集約通報や気象防災速報が追加された。暴風・波浪・大雪などは従来名のまま。
ネット上のサンプルコードおよびモデルの記憶は旧体系のものが大半である。

**情報名・電文コードをリテラルで書く場合は、必ず実物のフィード／技術資料で裏を取ること。**
裏が取れないものは推測で書かず、TODOコメントを残して確認を求める。

参照先:
- 情報の取得方法: https://xml.kishou.go.jp/xmlpull.html
- 技術資料: https://xml.kishou.go.jp/tec_material.html
- 電文一覧: https://xml.kishou.go.jp/xmllist.pdf
- 新体系: https://www.jma.go.jp/jma/kishou/know/bosai/keiho-update2026/

## アーキテクチャ

```
Scheduler(毎分) → poller ── Pub/Sub(jma-entries) ──→ processor ─→ 状態差分判定
                                                                    ├→ 即時通知
                                                                    └→ 集約バッファ
Scheduler(5分)  → digest ──────────────────────────────────────────→ 集約通知
```

- **poller**: フィードの条件付きGET → 新着 entry の検出 → Pub/Sub へ publish。ここで終わる。
  全国運用では1分間に数十件の電文が流れうるため、poller 内で電文本体を直列取得すると
  タイムアウトする。**poller は電文本体を取りに行かない。**
- **processor**: Pub/Sub 購読。電文取得 → パース → 状態差分 → 通知 or バッファ。
  並列に回るが、気象庁への同時接続は制限する
- **digest**: バッファを読み、集約通知を1通にまとめて送信、バッファをクリア

Pub/Sub は at-least-once なので、processor は冪等でなければならない。

## データソース

高頻度フィード（毎分更新、直近少なくとも10分ぶんを掲載）:

| 種別 | URL | 全国運用での扱い |
| --- | --- | --- |
| 定時 | `.../feed/regular.xml` | 記録のみ。通知しない |
| 随時 | `.../feed/extra.xml` | **主用途**。警報・注意報 |
| 地震火山 | `.../feed/eqvol.xml` | **主用途**。地震・津波・火山 |
| その他 | `.../feed/other.xml` | 台風情報等。要選別 |

ベースURL: `https://www.data.jma.go.jp/developer/xml/`

長期フィード（`*_l.xml`、毎時更新・数日ぶんの全入電）は、
(a) 流量の実測、(b) 障害復旧時の取りこぼし回収 に使う。通常運転では叩かない。

## 実装上の落とし穴（既知）

- **entry の `id` は電文XMLのURLそのもの。** 重複排除キーとしてそのまま使える
- **時刻表現が混在。** フィード全体の `updated` は `+09:00`、各 entry の `updated` は
  `Z`（UTC）。文字列比較すると9時間ずれる。必ず Date に正規化して比較する
- **ETagの保存は全エントリの publish 完了後。** 先に保存すると、処理中の例外で
  次回304が返り、そのぶんを恒久的に取りこぼす
- **`Head/InfoType` に 発表 / 訂正 / 取消 / 遅延 が入る。** 取消は状態差分の「解除」として
  扱う。無視すると解除済みの警報が出っぱなしに見える
- **`fast-xml-parser` は要素が1件のとき配列にしない。** entry も Area も必ず配列に正規化する
- **フィードの保持は約10分。** 毎分ポーリングなら約10回ぶんの猶予がある。
  数分の障害は次回実行で自然に回復する前提でよく、複雑なリトライは不要
- **Scheduler の毎分起動と関数タイムアウトを重ねない。** poller は 55秒 / attempt-deadline 60秒

## 技術スタック

- Node.js 22 / ESM（`"type": "module"`）
- `@google-cloud/functions-framework`、`@google-cloud/firestore`、`@google-cloud/pubsub`
- `fast-xml-parser`
- テスト: `node:test` + `node:assert`（追加依存を増やさない）
- リージョン: `asia-northeast1`

## コーディング方針

- 通知先はアダプタとして分離する（`notifiers/`）。console 実装と Slack 実装を先に用意し、
  LINE 等は後から差し込めるようにする
- 副作用（fetch / Firestore / Pub/Sub）と純粋なロジック（パース / 状態差分 / 深刻度判定）を
  分ける。純粋な側にテストを厚く書く。**状態差分と深刻度判定は必ず純粋関数にする**
- 閾値・情報名の分類は設定ファイルに切り出し、運用しながら調整できるようにする
- 環境変数はエントリポイントで集約し、各モジュールが `process.env` を直接読まない
- 秘密情報は Secret Manager 経由。`.env` をコミットしない
- 引き算を優先する。通知が確実に届き、かつ多すぎないことが最優先

## テスト方針

**実サーバーへアクセスするテストは書かない。** `test/fixtures/` に実データを保存して使う。

- 各フィードのAtom XML
- 電文: 気象警報・注意報（複数府県）、同一府県の連続する2電文（状態差分テスト用）、
  震源・震度に関する情報（同一EventIDの続報を含む）、津波、特別警報、取消電文

Firestore と Pub/Sub は公式エミュレータを使う。

## 未決事項（勝手に決めない）

1. 通知先（Slack / LINE Messaging API / メール）と宛先。全国運用では専用チャンネル推奨
2. 即時通知と集約通知の境界を、具体的にどの情報名で切るか
3. 集約通知の間隔（5分 / 15分）と、夜間の扱い
4. 注意報を記録対象に含めるか、完全に捨てるか
